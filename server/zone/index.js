const MapState = require('../zone')

/** Zone Server
 *  An instance of this exists for each zone that has active players
 */
module.exports = function(zoneData){
    this.state = MapState()
    this.mapData = 
}